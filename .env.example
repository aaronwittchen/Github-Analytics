# GitHub Configuration
GITHUB_TOKEN=''

# Cache Configuration
CACHE_TIMEOUT=300          # Cache timeout in seconds (5 minutes)
CACHE_MAX_ITEMS=100        # Maximum number of items in cache

# Repository Configuration  
MAX_REPOSITORIES=10        # Max repositories to return in user summary
LANGUAGE_STATS_LIMIT=10    # Max repositories to analyze for language stats
RANDOM_REPO_MAX_PAGES=33   # Max pages to search for random repository

# Server Configuration
PORT=3000
NODE_ENV=development
API_PREFIX=api
CORS_ORIGIN=http://localhost:3000,http://localhost:3001